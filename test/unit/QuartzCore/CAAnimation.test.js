'use strict'

import CAAnimation from '../../../src/js/QuartzCore/CAAnimation'
import * as Constants from '../constants'
import chai from '../../../node_modules/chai/chai'
import UnitTest from '../UnitTest'

const expect = chai.expect

const epsilon = 0.0001

/** @test {CAAnimation} */
describe('CAAnimation class', () => {
  UnitTest.classFunctionsDefined(CAAnimation, [
  ])

  UnitTest.functionsDefined(CAAnimation, [
  ])

  UnitTest.classPropertiesDefined(CAAnimation, [
  ])

  UnitTest.propertiesDefined(CAAnimation, [
  ])

  /** @test {CAAnimation#_basetimeFromTime} */
  describe('_basetimeFromTime', () => {
    it('should work', () => {
    })
  })

  /** @test {CAAnimation#_basetimeFromActivetime} */
  describe('_basetimeFromActivetime', () => {
    it('should work', () => {
    })
  })
})
