'use strict'

import CAMediaTimingFunction from '../../../src/js/QuartzCore/CAMediaTimingFunction'
import * as Constants from '../constants'
import chai from '../../../node_modules/chai/chai'
import UnitTest from '../UnitTest'

const expect = chai.expect

const epsilon = 0.0001

/** @test {CAMediaTimingFunction} */
describe('CAMediaTimingFunction class', () => {
  UnitTest.classFunctionsDefined(CAMediaTimingFunction, [
  ])

  UnitTest.functionsDefined(CAMediaTimingFunction, [
  ])

  UnitTest.classPropertiesDefined(CAMediaTimingFunction, [
  ])

  UnitTest.propertiesDefined(CAMediaTimingFunction, [
  ])

  /** @test {CAMediaTimingFunction#functionWithName} */
  describe('functionWithName', () => {
    it('should work', () => {
      
    })
  })

  /** @test {CAMediaTimingFunction#functionWithControlPoints} */
  describe('functionWithControlPoints', () => {
    it('should work', () => {
    })
  })

  /** @test {CAMediaTimingFunction#_getValueAtTime} */
  describe('_getValueAtTime', () => {
    it('should work', () => {

    })
  })
})
